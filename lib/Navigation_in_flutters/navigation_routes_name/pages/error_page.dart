import 'package:flutter/material.dart';

class eroor_page extends StatelessWidget {
  const eroor_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Error Page"),
    );
  }
}
